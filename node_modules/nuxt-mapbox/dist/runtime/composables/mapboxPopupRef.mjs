import { computed } from "vue";
import { useState } from "#imports";
export function useMapboxPopupRef(markerID) {
  return computed(() => {
    const popupInstances = useState("mapbox_popup_instances", () => {
      return {};
    });
    const marker = popupInstances.value[markerID];
    return marker;
  });
}
